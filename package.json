{
  "name": "spider-swap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "cypress:open": "cypress open",
    "cypress:run": "npx cypress run --browser chrome --headless",
    "cypress:parallel": "npx cypress run --browser chrome --headless --parallel",
    "cypress:sequential": "npx cypress run --browser chrome --headless --spec 'cypress/e2e/**/*.cy.{js,jsx,ts,tsx}'"
  },
  "devDependencies": {
    "cypress": "^14.3.0",
    "cypress-audit": "^1.1.0",
    "cypress-axe": "^1.6.0",
    "cypress-real-events": "^1.14.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0",
    "phan-wallet-mock": "^0.1.0"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.5.0"
  }
}
